
"use client"

import { ColumnDef } from "@tanstack/react-table"

import type { User } from "@/lib/types"

// This file is now empty as the table logic has been moved to user-table.tsx
// to better match the requested structure.
// You can define columns here if you decide to use tanstack-table again.
export const columns: ColumnDef<User>[] = []
